import { IconButton, MD3Colors } from 'react-native-paper';

export default function ActivityCardShareBtn() {
  return <IconButton icon="share-outline" iconColor={MD3Colors.error50} size={20} />;
}
