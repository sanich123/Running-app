export enum STATUSES {
  initial = 'initial',
  started = 'started',
  paused = 'paused',
  continued = 'continue',
}

export enum LANGUAGES {
  russian = 'russian',
  english = 'english',
}
