export const enum STATUSES {
  initial = 'initial',
  started = 'started',
  paused = 'paused',
  continue = 'continue',
}

export const enum LANGUAGES {
  russian = 'russian',
  english = 'english',
}
